# 复制为 .env 或 .env.production 后按需修改（Vite 仅暴露以 VITE_ 开头的变量）

# 后端 API 根地址（不要末尾斜杠）。
# 本地开发：复制为 .env.development（已随仓库提供）或在本文件取消注释下一行，与 backend uvicorn 一致
# VITE_API_BASE=http://127.0.0.1:8000
# 线上部署：填你已部署的 FastAPI 地址，例如
# VITE_API_BASE=https://zhidun.onrender.com

# 若使用 GitHub Pages 项目页子路径，需与 vite.config.js 里 base 一致，例如仓库名为 zhidun 时：
# VITE_BASE=/zhidun/
